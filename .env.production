# GitHub Pages: project is served in a *subfolder* (not site root). Relative base fixes
# asset resolution when the URL is redirected, trailing slash differs, or casing is normalized.
# Do not set an absolute /Repo/ path here unless you know it matches the live path exactly.
VITE_BASE_PATH=./
